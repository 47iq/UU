version: '3.1'

services:

  db:
    image: postgres
    restart: always
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=union
      - POSTGRES_PASSWORD=union
      - POSTGRES_DB=union

