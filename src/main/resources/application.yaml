spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: 123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create

urls:
  base:
    "api"
  auth:
    login:
      "login"
    register:
      "register"
    refresh:
      "refresh"
    base:
      "users"
  home:
    index:
      "index"
    create:
      "create"
    login:
      "login"
    details:
      "details"
    order:
      "order"
    basket:
      "basket"
    favourites:
      "favourites"
    orders:
      "orders"
    base:
      "home"
  items:
    item:
      "item"
    autocomplete:
      "autocomplete"
    base:
      "items"
